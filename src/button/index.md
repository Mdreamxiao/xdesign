# Button

This is an example component.

```jsx
import { Button } from 'xidesign';

export default () => (
  <>
    <Button size="small" style={{ color: 'red' }} />
    <Button size="normal" />
    <Button size="large" />
    <Button size="large" />
  </>
);
```
